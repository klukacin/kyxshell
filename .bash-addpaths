# Absolute path to this script.
echo SCRIPT=$(readlink -f ${BASH_SOURCE[0]})
# Absolute path this script is in.
echo SCRIPTPATH=`dirname ${BASH_SOURCE[0]}`

# Add mysql folder to path
PATH="$SCRIPTPATH/mysql:${PATH}"
export PATH
